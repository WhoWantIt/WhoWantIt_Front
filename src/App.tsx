import { BrowserRouter as Router } from "react-router-dom";
import RoutesConfig from "./route/router";

function App() {
  return (
    <Router>
      <RoutesConfig />
    </Router>
  );
}

export default App;
