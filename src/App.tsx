import { BrowserRouter as Router } from "react-router-dom";
import RoutesConfig from "./router/Router";

function App() {
  return (
    <Router>
      <RoutesConfig />
    </Router>
  );
}

export default App;
